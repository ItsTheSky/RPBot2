package info.itsthesky.api.maps.tiles;

import info.itsthesky.api.utils.Savable;
import org.jetbrains.annotations.Nullable;

/**
 * Represents a data of a tile.
 * <br>One tile can have multiple data, for example, a tile can have a type & an image.
 */
public interface TileData extends Savable {
}
